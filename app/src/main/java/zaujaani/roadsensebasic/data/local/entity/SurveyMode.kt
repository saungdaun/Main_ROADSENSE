package zaujaani.roadsensebasic.data.local.entity

enum class SurveyMode(val displayName: String) {
    GENERAL("Umum"),
    SDI("SDI — Surface Distress Index"),
    PCI("PCI — Pavement Condition Index")
}